server/ycf-send/.env
server/ycf-send/function.zip

# env files
.env
.env.local

# build output
dist/

# agent / local tooling
.claude/

# приватные документы объектов (реальные КП/сметы/ТЗ) — только локально, НЕ в публичный прод
object/
public/objects/

# бэкенд (Cloud Functions) — деплоится через yc, НЕ через этот публичный репо;
# содержит ID инфраструктуры (infra.json/deploy.ps1) — держим локально
server/

# исходники печатных шаблонов (Word-референсы) — не нужны для сборки
templates/

