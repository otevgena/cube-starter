# Чистый рефакторинг десктопа (clean-rebuild)

> Путь 3: переписываем ПК-версию начисто, блок за блоком, с текущим видом как
> эталоном. Мобилку делаем потом, на чистой основе. Ветка: `clean-rebuild` (от `main`).

## Принципы
- Никаких `!important` и пиксельных сдвигов (`left:-77px` и т.п.).
- Токены — в `tailwind.config.js` (цвета `ink/page/field/line/dark/gold`,
  шрифт `font-tight`, `max-w-container`, `h-header`).
- Каждый блок переписываем на чистый Tailwind, старые классы перестают
  использоваться → потом удаляем мёртвые куски из `index.css`.
- Легаси `index.css` пока подключён; новые компоненты его не используют,
  поэтому конфликтов нет (старые правила цепляются за старые классы/id).
- «Причесать» разрешено: убираем странности, вид ~98% как был.

## Сделано
- **Токены** в `tailwind.config.js`.
- **Шапка** (`src/components/layout/Header.jsx`) переписана начисто:
  - чистый Tailwind, без DOM-хаков (убрано перемещение лого/кнопок в панель),
    без пиксельных сдвигов;
  - `sticky top-0`, контейнер `max-w-container`, высота `h-header`;
  - навигация, поиск, действия (Вход/Регистрация или аватар, «Ищу работу»,
    «Оставить заявку») — сохранена вся логика авторизации;
  - «Услуги» → чистая выпадающая панель (5 категорий + услуги активной),
    вместо мегапанели с фикс-размерами;
  - id `site-header-static` убран → старые правила `#site-header*` в index.css
    стали мёртвыми (на новую шапку не влияют).
  - На узких экранах пока простой бургер (открывает панель услуг) —
    полноценная мобилка позже.

## Готово (продолжение)
- **Шапка** доведена до awwwards-стиля: панель «Услуги» = плавающая карточка
  (портал в body), фон #ededed поджат к содержимому, логотип в левом жёлобе,
  поиск+категории с одного края, список заканчивается по концу поиска, действия
  в правом жёлобе, сокращения справа у услуг (ТУ/кВт/СКС…), плавное переключение
  категорий, scrollbar-gutter:stable (без сдвига фона). Шапка широкая (1700),
  не sticky, без нижней полоски.
- **HomeMain** (первый экран) — переписан на токенах: мета-строка, заголовок
  CUBE-TECH (clamp), подпись директора с анимированным подчёркиванием, «ромб»
  `aspect-[1441/1112] w-[min(1441px,96vw)]` с тёмной рамкой (padding clamp).
  Легаси `.about-hero-*` пока живут (нужны Contact) — почистить позже.

- **Services** (секция «Услуги» на главной) — переписана начисто: заголовок
  УСЛУГИ + гифка на «И», «Делегируйте всё…», 5 карточек (картинка с ховером →
  заголовок → «Подробно» с жёлтым подчёркиванием → 3 иконки-фичи), «Каждая деталь
  имеет → Значение». Убраны ~400 строк хелперов и анимированные <object>-SVG
  (иконки стали обычными <img> → ушли ошибки classList в консоли).
  ВАЖНО: в index.css есть `body.home #services{display:none}`, но по факту секция
  видна на главной — оставил как есть; при чистке index.css решить судьбу правила.

- **About** (#about) — переписан: Обзор/УЗНАТЬ БОЛЬШЕ/О НАС (винт-гифка),
  крупные абзацы, Команда + Бюрократия (раскрывашки), Результат. Картинка about1
  убрана. Отступ метка↔большой-заголовок = 26px (фирменный, держим одинаковым).
- **Projects** (#projects) — переписан: шапка Директория/ПРОЕКТЫ, 2 карточки
  проектов (мини-карусель: свайп/точки/кроссфейд, Дней-бокс), таблица
  (Заказчик/Название/Год/Услуги/Подробнее, пунктир), центровой блок «26 проектов».

- **Contact** (#contact) — переписан: заголовок Напишите нам/КОНТАКТЫ + форма
  (имя/почта/телефон/кастомный селект/комментарий/чекбокс/кнопка), валидация и
  отправка в Yandex Cloud сохранены 1-в-1, тост успеха. Убраны легаси
  `.about-hero`/`.contact-hero` — теперь `.about-hero-*` в index.css больше
  никем не используются (можно чистить).

- **Reviews** (#reviews) — переписан: шапка Портфолио/ОТЗЫВЫ + 2 карточки-скана
  (тёмный фон, «Читать отзыв» → попап ReviewPopup, «Напишите нам»).
- **Поиск по сайту** — src/data/search-index.ts (индекс+ранжирование), интеграция
  в шапку (клик открывает панель, ввод → результаты), подсветка совпадений
  морковным (токен `carrot` #FA5D29). Страница /contact = блок Contact (DRY).

- **Footer** (`src/components/layout/Footer.jsx`) — переписан на поток (flex)
  вместо абсолютного позиционирования; ссылки в массивах NAV/LEGAL; токены.
- **StickyDock** (`src/components/common/StickyDock.jsx`) — все стили собраны
  в компонент (без `!important`); из `index.css` удалён легаси dock-водопад
  (две полные копии + слои переопределений, −582 строки). Режимы: ровная
  рамка 6px в electro/contact; `c.` вне главной → на главную (на главной →
  наверх); `/contact` = плашка только с `c.`; «Оставить заявку» со страницы
  услуги → `/contact` с предвыбором услуги (`sessionStorage` → форма);
  плавная анимация ширины при смене режима.

- **Чистка `index.css`** — удалён мёртвый CSS старой шапки (mega-panel,
  `.svc-*`, `.nav-link`, `.search-*`, `.btn-*`, `.logo-c`, `.badge-new`,
  `#site-header*`, пиксельные сдвиги `.min-h-dvh …`, dock-водопад). Файл
  **1908 → 334 строки**. Живое сохранено: `.about-hero-*` (нужны legal/услугам/
  pro/projects/модалкам), font-smoothing, `padding main`, токены `:root`,
  preloader, `body.home #services`, `.contact-hero` фон, `.container-wide`.

- **Legal-страницы** (terms/cookies/privacy) — общий `legal-shell.jsx`
  (вкладки + заголовок clamp + слоган + хелперы H3/P/UL/OL/LI/B/A); три
  страницы стали тонкими (только текст), без дублей `<style>` и `.about-hero-title`.
- **Страницы услуг (категории)** — общий `services-shell.jsx`
  (`ServiceCategoryPage`: вкладки/заголовок/слоган/вступление/таблица/график/
  CUBE-индекс); 5 страниц = тонкие конфиги (~2200 строк дублей → шелл + 5×~50).
  Ховер «Подробнее» = как «Оставить заявку» в шапке.
  ВНИМАНИЕ: детальные страницы электрики (power-connection/power-upgrade/indoor)
  НЕ трогаем — заказчик переделывает их целиком (кроме оглавления).
- **Dev-авторизация** (временно, только dev): `src/lib/dev-auth.js` —
  fetch-шим `/auth/*` и `/admin/*` без бэка. Аккаунты: admin@cube.ru/1234,
  user@cube.ru/1234. Подключение в `main.jsx` под `import.meta.env.DEV`.
  Гард `/account/*` + редирект на главную при выходе (App.jsx).
  Док: режим аккаунта (c. + аватар+имя + «Настройки» #aceec4). Меню аватара
  по роли. УБРАТЬ ПЕРЕД ПРОДОМ: файл dev-auth.js + вызов в main.jsx.

## TODO (десктоп) — по желанию заказчика, пока на паузе
- Сами страницы аккаунта (Профиль/Настройки/Администратор) — заказчика пока
  устраивает как есть; причешем по его правкам позже.
- pro (Вакансии) / projects (Проекты) — ещё на легаси `.about-hero-*`/`container-wide`.
- Детальные страницы услуг электрики — ждём переделку заказчика.
- Глянуть судьбу `.min-h-dvh > main { padding-top }` и `body.home #services`.

## Потом
- Мобильная адаптация на чистой основе (наработки из ветки
  `mobile-adaptation` — как референс).
- Убрать `node_modules` из git (отдельная уборка).

## Эталон / проверка
Текущий вид — ветка `main` (живой сайт cube-tech.ru) и
`git diff main...clean-rebuild`. Проверять на полной ширине (десктоп).
